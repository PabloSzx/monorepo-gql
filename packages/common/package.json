{
  "name": "common",
  "version": "1.0.0",
  "description": "common",
  "license": "MIT",
  "author": "PabloSz",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --emitDeclarationOnly && tsup-globby src -d dist --minify",
    "dev": "nodemon",
    "dev:nodemon": "concurrently \"tsup-globby src -d dist\" \"tsc --emitDeclarationOnly\""
  },
  "dependencies": {
    "tsup-globby": "^1.0.1"
  },
  "devDependencies": {
    "@jest/types": "^26.6.1",
    "@types/node": "^14.14.2",
    "typescript": "^4.0.3",
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6"
  }
}

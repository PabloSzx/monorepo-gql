{
  "name": "common",
  "version": "1.0.0",
  "description": "common",
  "license": "MIT",
  "author": "PabloSz",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --emitDeclarationOnly && tsup-globby src -d dist --minify",
    "test": "exit 0"
  },
  "dependencies": {
    "tsup-globby": "^1.0.1"
  },
  "devDependencies": {
    "@jest/types": "^26.6.1",
    "@types/node": "^14.14.2",
    "typescript": "^4.0.3"
  }
}
